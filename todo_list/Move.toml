[package]
name = "todo_list"
edition = "2024.beta"

[dependencies]
# Remove explicit Sui dependency - let Sui handle it automatically

[addresses]
todo_list = "0x0"

# Named addresses will be accessible in Move as `@name`. They're also exported:
# for example, `std = "0x1"` is exported by the Standard Library.
# alice = "0xA11CE"
